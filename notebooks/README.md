# notebooks/

Notebooks de Jupyter, **una subcarpeta por integrante**: `julian/`, `emilio/`, `ivan/`.

Trabajá solo en tu subcarpeta para evitar conflictos de git. Antes de commitear, hacé *restart & run-all* para que las salidas guardadas coincidan con el código. Ver `docs/GUIDELINES.md`.
